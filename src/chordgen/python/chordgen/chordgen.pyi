from typing import List

def render_chord_progression(
        chords: List[List[str]],
        target_samplepack_path: str,
        output_file_name: str,
        output_file_path: str
    ) -> str: ...